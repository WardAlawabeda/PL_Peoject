import 'package:flutter/material.dart';

class FurnitureStoreList extends StatelessWidget {
  const FurnitureStoreList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('furniture'),
    );
  }
}