import 'package:flutter/material.dart';

class SportsStoreList extends StatelessWidget {
  const SportsStoreList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('sports'),
    );
  }
}