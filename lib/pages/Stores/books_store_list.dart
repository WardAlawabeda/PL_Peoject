import 'package:flutter/material.dart';

// TODO : make the service to get the stores list

class BooksStoreList extends StatelessWidget {
  const BooksStoreList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('books'),
    );
  }
}