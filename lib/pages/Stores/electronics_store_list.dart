import 'package:flutter/material.dart';

class ElectronicsStoreList extends StatelessWidget {
  const ElectronicsStoreList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('electronics'),
    );
  }
}