import 'package:flutter/material.dart';

class FashionStoreList extends StatelessWidget {
  const FashionStoreList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('fashion'),
    );
  }
}