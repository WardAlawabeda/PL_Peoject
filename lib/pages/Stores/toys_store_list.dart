import 'package:flutter/material.dart';

class ToysStoreList extends StatelessWidget {
  const ToysStoreList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('toys'),
    );
  }
}